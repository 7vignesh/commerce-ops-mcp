# PostgreSQL connection string.
# Any Postgres instance works. Managed providers usually require SSL, which the
# server enables automatically for Supabase hosts.
DATABASE_URL=postgresql://user:password@host:5432/database

# Port the MCP server listens on. Defaults to 3000.
PORT=3000
